/*
 * NotificationComponent Messages
 *
 * This contains all the text for the NotificationComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NotificationComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NotificationComponent component!',
  },
});
