// import { selectSingleArticleDomain } from '../selectors';

describe('selectSingleArticleDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
