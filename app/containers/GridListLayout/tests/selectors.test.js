// import { selectGridListLayoutDomain } from '../selectors';

describe('selectGridListLayoutDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
